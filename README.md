# url-shortener
A backend URL Shortener service built with Node.js, Express.js, and MongoDB that generates unique short links, stores URLs in a database, and redirects users to the original link while tracking usage.
